# Docs

This folder keeps local repair notes and operational writeups for the Windows `easy_llamacpp` launcher setup.

## Documents

- The launcher now uses a single `llama-server` with optional `--mmproj` on the primary service.
- Keep new notes here only when they still match that single-service design.
